# Verification screenshots

Local manual / agent screenshots (`manual-*.png`, `bsh*-*.png`) stay at the
repo root only as working files — they are gitignored.

Commit a screenshot here only when a ticket needs a durable fixture (for
example a visual regression baseline). Prefer deleting one-off captures
after the review instead of checking them in.
